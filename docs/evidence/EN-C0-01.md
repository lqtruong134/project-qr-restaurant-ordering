# EN-C0-01 — Biên bản kiểm chứng local

Ngày: 09/09/2026. Trạng thái: In Review; chưa được PO nghiệm thu.
Repository: /home/quyettruong05/projects/qr-ordering-thesis
Branch: main; chưa có commit/remote vì đang chờ thông tin tác giả và GitHub từ chủ dự án.

## Kết quả đã chạy

- pnpm install --frozen-lockfile: PASS.
- pnpm lint: PASS, không có warning.
- pnpm typecheck: PASS cho các package, web/API và công cụ/test.
- pnpm test: PASS 3/3.
- pnpm test:integration: PASS 1/1 trên PostgreSQL thật.
- pnpm db:check: PASS (SELECT 1).
- pnpm check:secrets: PASS kiểm tra cơ bản; .env được Git bỏ qua, không lộ mật khẩu local vào source.
- pnpm build: PASS, Next.js 16.3.4.
- pnpm test:e2e: PASS 2/2 (desktop 1440×1000, mobile 390×844), không lỗi JavaScript hoặc tràn ngang.
- PostgreSQL 18.6: healthy, cổng 127.0.0.1:5433, volume qr-ordering-thesis_postgres_data.
- Dừng postgres dự án: readiness 503 và thông báo an toàn; khởi động lại healthy, SELECT 1 PASS.
- my-postgres và volume cũ giữ nguyên.

Lockfile SHA-256: 0863325f5e53139093e365a862bca3c748e3f6e64d98c7a45d2c82d68c56b0b4

## Giới hạn và việc tiếp theo

- CI workflow đã viết; chưa có kết quả GitHub Actions do chưa có remote.
- Kiểm tra secret cơ bản không phải audit bảo mật toàn diện.
- Chưa có schema/migration 14 bảng, seed nghiệp vụ, auth/RBAC, Data Dictionary; thuộc EN-C0-02, US-C0-01, EN-C0-03.
- Prisma chỉ được cấu hình, chưa generate client khi chưa có model.
- Môi trường local mới được dựng từ đầu và chạy được. Rehearsal toàn Sprint hai lần thực hiện khi đủ schema/auth/seed.
- PO cần chạy thử theo README và xác nhận trước khi bật Đạt DoD.
